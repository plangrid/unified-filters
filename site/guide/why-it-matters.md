# Why it matters

Every list in the product filters the same way — the same filters presentations (Filters Panel, Quick Filters, Saved Filters) — across Issues, RFIs, Forms and more, on both iOS and Android.

> Users learn filtering **once** — and every tool behaves the same.

## What it gives the product

<div class="give"><span class="gn">Consistency</span><span class="gd">One mental model — every tool filters the same way.</span></div>
<div class="give"><span class="gn">Platform parity</span><span class="gd">Identical behavior on iOS and Android.</span></div>
<div class="give"><span class="gn">Free for new tools</span><span class="gd">Teams plug in — they don't rebuild filtering each time.</span></div>
<div class="give"><span class="gn">Persistent filters</span><span class="gd">A user's filters persist per list when they return to their view.</span></div>
<div class="give"><span class="gn">Built-in insight</span><span class="gd">Filter usage is tracked automatically — no extra wiring.</span></div>

Next: see the [Components overview](../components/), or jump straight to the [architecture](../architecture/architecture-at-a-glance).
