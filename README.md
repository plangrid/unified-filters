# Unified Filters

Everything for the Unified Filters Framework — cross-platform (iOS + Android)
shared filtering (Filters Panel, Quick Filters, Saved Filters).

- **[`deck/`](./deck)** — the Slidev onboarding/handover deck.
- **[`site/`](./site)** — the VitePress reference portal (architecture, components, integration guide).

Published via GitHub Pages — see `deploy.sh` for how both get built and pushed together.
